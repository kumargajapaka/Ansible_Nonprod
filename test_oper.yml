---
 - name: This is about test operators
   hosts: localhost 
   gather_facts: flase 
   vars: 
    x: 25
   tasks: 
   - debug: 
       msg: 
        - "The value of x is: {{x}}"
        - "The value of x is: {{y}}"
     when: y is defined